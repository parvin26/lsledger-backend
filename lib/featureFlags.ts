/**
 * When true, all visitors are treated as signed in and use NEXT_PUBLIC_TEST_ACCESS_TOKEN
 * for API calls. No real multi-user isolation; for V1 testing only.
 */
export const GUEST_MODE_ENABLED = true
